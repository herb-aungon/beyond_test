{% extends "home.html" %}
{% block start %}
   {{ super() }}

	  <!-- <textarea rows="4" cols="50"> -->
	  <!--   {{ hash_tags }} -->
	  <!-- </textarea> -->

	  <!-- <textarea rows="4" cols="50"> -->
	  <!--   {{ test }} -->
	  <!-- </textarea> -->
	  {% with messages = get_flashed_messages() %}
	  {% if messages %}
	  {% for message in messages %}
	  <div class="alert alert-danger">
	    <a href="" class="close" data-dismiss="alert" aria-label="close">&times;</a>
	    <strong> <a>{{ message }}</a></strong>
	  </div>
	  {% endfor %}
	  {% endif %}
	  {% endwith %}
	  <form class="form-inline" role="form">
	    <legend>Create Hash Tag Battle</legend>
	    <div class="form-group">
	      <label class="sr-only" for="input_batt_name">Battle Name</label>
	      <input type="text" class="form-control" id="battle_name" placeholder="battle name" style="text-transform:lowercase;" />
	    </div>
	    <div class="form-group">
	      <label class="sr-only" for="input_start">Start Date</label>
	      <input type="text" class="form-control" id="start" placeholder="start_date yy/mm/dd" style="text-transform:lowercase;" />
	    </div>
	    <div class="form-group">
	      <label class="sr-only" for="input_end">End Date</label>
	      <input type="text" class="form-control" id="end" placeholder="end_date yy/mm/dd" style="text-transform:lowercase;" />
	    </div><br>
	    <br>
	    <button id="input_submit" name="input_submit" class="btn btn-success">Create Battle</button>
	  </form>
	  
	  <h2>Hash Tags</h2>
	  {% if hash_tags %}
	  <table id="hash" class="table table-hover" >
	    <thead>
	      <tr>
	  	<th>HashTags</th>
	  	<th>Date</th>
		<th>Tweet Tagged to:</th>
	      </tr>
	    </thead>
	    <tbody>
	      {% for i in hash_tags %}
	      <tr>
	        <td>{{ i.hash_tag }}</td>
	        <td  >{{ i.date }}</td>
		<td  >{{ i.user }}</td>
	      </tr>
	      {% endfor %}
	    </tbody>
	  </table>
	  {% endif %}
	  
{% endblock %}
